#include "main.h"
/**
*_abs - function that comutes the absolute value
*@n: the int to check
*Return: the absolute value
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
